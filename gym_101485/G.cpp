#include<bits/stdc++.h>

using namespace std;

int main() {

    cin >> n;
    for (int i = 1;i <= n; i++) {
        cin >> a[i];
    }
    for (int i = 1;i <= n; i++) {
       cin >> b[i];
       posb[b[i]] = i;
    }

    for (int i = 1; i <= n; i++) {
        cin >> c[i];
        posc[c[i]] = i;
    }

    for (int i = 1; )



}
