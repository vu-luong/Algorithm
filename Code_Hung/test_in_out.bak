var
        s1, s2: string;

begin

        assign(input, 'input.txt');
        reset(input);
        assign(output, 'output.txt');
        rewrite(output);


        read(s1, s2);

        writeln(s1);
        writeln(s2);
        readln;

        close(input);
        close(output);
end.
